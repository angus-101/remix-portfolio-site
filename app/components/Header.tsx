import logo from "../images/logo.png";

export const Header = () => (
    <div>
        <img src={logo} alt="Website logo. Capital M with a line across the left half to look like a capital A."/>
    </div>
)
